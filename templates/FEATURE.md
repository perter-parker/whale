# FEATURE: <모듈/피처명>

> 이 파일은 모듈의 **경계 계약**이다. researcher 최우선 필독, planner 영향범위 판정 근거.
> 부분수정 방지: "이 로직은 여기가 주인"을 명시해 같은 로직을 딴 데서 놓치는 걸 막는다.

## Owns (이 모듈이 소유·책임지는 것)
- <엔티티/유스케이스/화면/규칙 — 이 모듈이 진실원인 것>

## Does NOT own (여기 없음 — 남의 것)
- <다른 모듈 소유 항목 + 소유 모듈명. 여기서 중복 구현 금지>

## 진입점 (Entry points)
| 유형 | 식별자 | 위치 |
|------|--------|------|
| API | <METHOD /path> | <Controller 경로> |
| 화면/라우트 | <route> | <page 경로> |
| 이벤트 핸들러 | <이벤트명> | <경로> |

## 발행 이벤트 (Emits)
| 이벤트 | 페이로드 | 구독자(알려진) |
|--------|----------|----------------|

## 외부 의존성 (Depends on)
| 대상 | 방식 | 계약 SSOT |
|------|------|-----------|
| <타 BC/모듈> | <ID 참조 + App Service \| API \| 이벤트> | <계약 위치> |

> BC 간 참조는 FK 없이 ID + Application Service 조회.

## 계약 SSOT (이 모듈이 노출하는 계약)
- <OpenAPI 경로 / 스키마 / 타입 원본> — 변경 시 파생물 동반 갱신(드리프트 금지)

## 불변식 / 비즈니스 규칙 (BR)
- <BR-ID: 규칙> (테스트: <경로>)

## 변경 시 주의 (Change surface hints)
- <이 모듈을 바꾸면 함께 봐야 할 곳 — 부분수정 방지 힌트>
